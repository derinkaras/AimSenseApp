import React from 'react';
import { View, Text } from 'react-native';

const Home = () => {
  return (
    <View className="flex-1 bg-brand-black">
      <Text>Dashboard</Text>
    </View>
  );
};

export default Home;
